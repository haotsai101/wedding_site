import React from 'react';

const Registry = () => {
    return (
        <div className='h-96'>
            <a id="registry"/>
            Registry
        </div>
    );
};

export default Registry;
